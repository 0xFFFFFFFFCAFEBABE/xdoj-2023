#include <stdio.h>int main() {    int n, m;    int hash[16] = {0};    scanf("%d %d", &n, &m);    for (int i = 0; i < n; i++) {        for (int j = 0; j < m; j++) {            int temp;            scanf("%d", &temp);            hash[temp]++;        }    }    for (int i = 0; i < 16; i++) {        if (hash[i] == 0) continue;        printf("%d %d\n", i, hash[i]);    }    return 0;}