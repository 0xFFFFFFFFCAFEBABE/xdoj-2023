#include <stdio.h>int main() {    int n, m,i,j;    int hash[16] = {0};    scanf("%d %d", &n, &m);    for (i = 0; i < n; i++) {        for (j = 0; j < m; j++) {            int temp;            scanf("%d", &temp);            hash[temp]++;        }    }    for (i = 0; i < 16; i++) {        if (hash[i] == 0) continue;        printf("%d %d\n", i, hash[i]);    }    return 0;}