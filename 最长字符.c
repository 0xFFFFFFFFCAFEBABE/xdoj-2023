#include <stdio.h>#include <string.h>#include <memory.h>int main() {    char save[101];    char buf[101];    gets(buf);    int longest = 0;    while (strcmp(buf, "***end***")) {        int temp = strlen(buf);        if (temp > longest) {            longest = temp;            memcpy(save, buf, temp + 1);        }        gets(buf);    }    printf("%d\n", longest);    printf("%s", save);    return 0;}