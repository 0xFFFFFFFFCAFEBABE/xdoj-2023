#include <stdio.h>#include <math.h>int main() {    int m, n;    scanf("%d %d", &m, &n);    for (int i = 0; i < m; i++) {        int combo = 0;        int start = 0;        int end = 0;        int max_combo = 0;        int max_s = 0, max_e = 0;        for (int j = 0; j < n; j++) {            int temp;            scanf("%d", &temp);            if (temp == 1) {                if (combo == 0) {                    start = j;                }                end = j;                combo++;                if (combo > max_combo) {                    max_s = start;                    max_e = end;                    max_combo = combo;                }            } else {                if (combo > max_combo) {                    max_s = start;                    max_e = j;                    max_combo = combo;                }                combo = 0;            }        }        if (max_combo == 0 && combo == 0) {            printf("-1 -1\n");        } else {            if (max_combo == 0) {                max_s = start;                max_e = end;            }            printf("%d %d\n", max_s, max_e);        }    }    return 0;}