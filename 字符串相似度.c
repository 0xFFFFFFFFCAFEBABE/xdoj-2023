#include <stdio.h>#include <string.h>#include <ctype.h>#include <minmax.h>int main() {    char s1[114];    char s2[114];    int dp[101][101] = {0};    gets(s1);    gets(s2);    int n1 = strlen(s1);    int n2 = strlen(s2);    int LCS = 0;    for (int i = 1; i <= n1; i++) {        for (int j = 1; j <= n2; ++j) {            if (tolower(s1[i - 1]) == tolower(s2[j - 1])) {                dp[i][j] = dp[i - 1][j - 1] + 1;                if (dp[i][j] > LCS) {                    LCS = dp[i][j];                }            } else {                dp[i][j] = 0;            }        }    }    double sim = (2.0 * LCS) / (n1 + n2);    printf("%.3lf", sim);    return 0;}