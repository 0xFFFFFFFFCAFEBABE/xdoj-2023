#include <stdio.h>#include <stdlib.h>#include <memory.h>int main() {    int n;    scanf("%d", &n);    int mat[11][11] = {0};    for (int i = 0; i < n; i++) {        mat[i][0] = 1;    }    for (int j = 1; j < n; j++) {        for (int i = 1; i < n; i++) {            mat[i][j] = mat[i - 1][j] + mat[i - 1][j - 1];        }    }    for (int i = 0; i < n; i++) {        for (int j = 0; j < n; j++) {            if (!mat[i][j]) {                continue;            }            printf("%d ", mat[i][j]);        }        printf("\n");    }    return 0;}