#include <stdio.h>#include <stdlib.h>#include <memory.h>int main() {    int n;    int hash[101][101] = {0};    int sum = 0;    scanf("%d", &n);    for (int i = 0; i < n; i++) {        int x1, y1, x2, y2;        scanf("%d %d %d %d", &x1, &y1, &x2, &y2);        for (int j = x1; j < x2; j++) {            for (int k = y1; k < y2; k++) {                if (hash[j][k] == 0) {                    sum++;                }                hash[j][k] = 1;            }        }    }    printf("%d", sum);    return 0;}