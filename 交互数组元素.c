#include <stdio.h>#include <memory.h>#include <string.h>#include <minmax.h>int main() {    int n;    scanf("%d", &n);    int save[51];    for (int i = 0; i < n; i++) {        scanf("%d", &save[i]);    }    for (int i = 0; i < n; i++) {        int temp;        scanf("%d", &temp);        printf("%d ", temp);    }    for (int i = 0; i < n; i++) {        printf("%d ", save[i]);    }    return 0;}