#include <stdio.h>#include <string.h>#include <stdlib.h>struct data {    int num;    char country[21];};int cmp(const void*a, const void*b){    struct data* p1 = (struct data*)a;    struct data* p2 = (struct data*)b;    return p2->num - p1->num;}int main() {    struct data countries[101];    int n;    scanf("%d", &n);    for(int i = 0;i<n;i++)    {        char name[20] = {0};        int temp;        scanf("%s %d", name, &temp);        memcpy(countries[i].country, name, strlen(name) + 1);        countries[i].num = temp;    }    qsort(countries, n, sizeof(struct data), cmp);    for(int i = 0;i<n;i++)    {        printf("%s %d\n", countries[i].country, countries[i].num);    }    return 0;}