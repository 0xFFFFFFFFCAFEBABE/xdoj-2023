#include <stdio.h>#include <math.h>int getRev(int num) {    int result = 0;    int temp = num;    while (temp != 0) {        result *= 10;        result += temp % 10;        temp /= 10;    }    return result;}int main() {    int num;    scanf("%d", &num);    int rev = getRev(num);    if (rev == num) {        int sum = 0;        while (num != 0) {            sum += num % 10;            num /= 10;        }        printf("%d", sum);    } else {        printf("no");    }    return 0;}