#include <stdio.h>#include <memory.h>#include <string.h>#include <minmax.h>#include <stdlib.h>int cmp(const void *a, const void *b) {    return *(int *) b - *(int *) a;}int main() {    int m;    scanf("%d", &m);    int mat[21][21];    int results[50];    int index = 0;    int main_x = 0;    int nmain_x = 0;    for (int i = 0; i < m; i++) {        int sum = 0;        for (int j = 0; j < m; j++) {            scanf("%d", &mat[i][j]);            if (i == j) {                main_x += mat[i][j];            } else if (i + j == m - 1) {                nmain_x += mat[i][j];            }            sum += mat[i][j];        }        results[index] = sum;        index++;    }    for (int i = 0; i < m; i++) {        int sum = 0;        for (int j = 0; j < m; j++) {            sum += mat[j][i];        }        results[index] = sum;        index++;    }    results[index] = main_x;    index++;    results[index] = nmain_x;    index++;    qsort(results, index, 4, cmp);    for (int i = 0; i < index; i++) {        printf("%d ", results[i]);    }    return 0;}