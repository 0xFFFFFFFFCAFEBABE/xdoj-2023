#include <stdio.h>#include <memory.h>#include <string.h>#include <minmax.h>int main() {    int n, m;    scanf("%d %d", &n, &m);    int s[101][101];    for (int i = 0; i < n; i++) {        for (int j = 0; j < m; j++) {            scanf("%d", &s[i][j]);        }    }    int c[101][101];    for (int i = 0; i < m; i++) {        for (int j = 0; j < n; j++) {            c[i][j] = s[n - j - 1][i];        }    }    for (int i = 0; i < m; i++) {        for (int j = 0; j < n; j++) {            printf("%d ", c[i][j]);        }        printf("\n");    }    return 0;}