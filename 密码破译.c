#include <stdio.h>int main() {    char src[1145] = {0};    scanf("%s", &src);    int i = 0;    while (src[i]) {        char raw = src[i];        if(raw >= 'a' && raw <= 'z')        {            raw +=4;            raw = (raw - 'a') % 26 + 'a';        }        else        {            raw +=4;            raw = (raw - 'A') % 26 + 'A';        }        printf("%c", raw);        i++;    }    return 0;}